# TeamHarness Python 依赖（从 pyproject.toml [project].dependencies 生成）
# 服务端 + 客户端共用

# Web 框架
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27

# 数据模型
pydantic>=2.6

# YAML 解析（INDEX.md frontmatter / categories.yaml / mapping.yaml）
PyYAML>=6.0

# 数据库（核心）
SQLAlchemy[asyncio]>=2.0.27
alembic>=1.13

# 生产 PG 驱动（异步 + 同步，docker-compose DATABASE_URL 用 psycopg）
asyncpg>=0.29
psycopg[binary]>=3.1

# 向量库
qdrant-client>=1.8

# 签名验证（tool 资产 PR Review 强制 Ed25519 验签）
cryptography>=42.0
