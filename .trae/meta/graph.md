# TeamHarness 项目规则图谱索引

> 本文件是 TeamHarness 项目级规则图谱，登记本项目 `.trae/rules/gotchas.md` 中的规则节点及其关系。
> 全局通用规则图谱见 `~/.trae-cn/meta/graph.md`。
> 修改项目规则时，沿此图谱查找关联节点并连锁更新；同时检查全局图谱是否有关联节点。
> 详细机制见 `~/.trae-cn/skills/resource-harness/SKILL.md` 环节四。

## 节点

### 项目规则节点

| 节点ID | 名称 | 分类 | gotchas.md 位置 | 全局图谱登记 | 状态 |
|--------|------|------|----------------|-------------|------|
| R036 | 测试注入路径与生产注入路径一致性 | API 设计 | §API 设计 → 测试注入路径与生产注入路径一致性 | 是（项目级） | 活跃 |
| R041 | 资产归属验证（owner 与 API Key 一致性）| 后端资产访问控制 | §后端资产访问控制 → 资产归属验证 | 是（项目级） | 活跃 |
| R042 | ACL 入参格式校验（grantee_id 格式验证）| 后端资产访问控制 | §后端资产访问控制 → ACL 入参格式校验 | 是（项目级） | 活跃 |
| R043 | 资产可见性与关系管理（ACL + 图结构）| 后端资产访问控制 | §后端资产访问控制 → 资产可见性与关系管理 | 是（项目级） | 活跃 |
| R044 | HTTP 请求层安全（401 拦截 + 超时控制）| 前端安全与健壮性 | §前端安全与健壮性 → HTTP 请求层安全 | 是（项目级） | 活跃 |
| R045 | 表单提交重入控制（handleUpdateScope 重入守卫）| 前端安全与健壮性 | §前端安全与健壮性 → 表单提交重入控制 | 是（项目级） | 活跃 |
| R046 | Vue 生命周期异步声明（onMounted + await）| 前端安全与健壮性 | §前端安全与健壮性 → Vue 生命周期异步声明 | 是（项目级） | 活跃 |
| R047 | 登录态异步校验与浏览器导航集成 | 前端安全与健壮性 | §前端安全与健壮性 → 登录态异步校验与浏览器导航集成 | 是（项目级） | 活跃 |
| R048 | 资产同步与代码审查门禁 | 工程流程治理 | §工程流程治理 → 资产同步与代码审查门禁 | 是（项目级） | 活跃 |
| R049 | Gitea 分支保护 | 工程流程治理 | §工程流程治理 → Gitea 分支保护 | 是（项目级） | 活跃 |

### 关联的全局规则节点

> 以下全局规则源自 TeamHarness 项目，已迁移至全局规则文件，但与本项目代码强相关。
> 2026-08-19 起核心高频规则（fastapi/db/pytest/python）已复制到项目级 `.trae/rules/`，TRAE 自动加载；browser/terminal 留全局按需读取。

| 全局节点ID | 名称 | 全局位置 | 项目级副本位置 | 与项目的关系 |
|-----------|------|---------|--------------|------------|
| R025 | 服务类 ↔ HTTP 路由注册一致性（契约三件套）| rules/fastapi-rules.md | `.trae/rules/fastapi-rules.md`（自动加载）| 源自 TeamHarness 多 Agent 协作，适用于本项目所有 FastAPI 路由注册场景 |
| R028 | 参数传递完整性（注入 + 透传 + 契约同步）| rules/python-rules.md | `.trae/rules/python-rules.md`（自动加载）| 源自 TeamHarness 多 Agent 协作，与 R036 互补（R036 管注入路径，R028 管参数传递）|
| R031 | import 语句不支持条件表达式 | rules/python-rules.md | `.trae/rules/python-rules.md`（自动加载）| 源自 TeamHarness 多 Agent 协作 |
| R035 | 冷启动期功能容错 | rules/python-rules.md | `.trae/rules/python-rules.md`（自动加载）| 源自 TeamHarness 多 Agent 协作，适用于本项目的聚类 / 召回模块 |
| R037 | 配置文件路由表 ↔ 代码 router 一致性校验 | rules/fastapi-rules.md | `.trae/rules/fastapi-rules.md`（自动加载）| 源自 TeamHarness，适用于本项目 nginx + FastAPI 架构 |
| R038 | 测试 DB 后端必须覆盖生产 DB 驱动与特性 | rules/db-rules.md | `.trae/rules/db-rules.md`（自动加载）| 适用于本项目 SQLite 测试 + PG 生产架构 |

## 关系

| 源节点 | 关系类型 | 目标节点 | 说明 |
|--------|---------|---------|------|
| R036 | 互补 | R028 | R036 管注入路径一致性，R028 管参数传递完整性，两者共同覆盖"Service 注入 + 参数传递"场景 |
| R036 | 互补 | R025 | R036 管注入路径，R025 管路由注册，两者共同覆盖"Service 实现 → 注入 → 路由注册"全链路 |
| R041 | 互补 | R043 | R041 管 owner 校验，R043 管 ACL 可见性，两者共同覆盖"资产归属 + 细粒度授权"双层访问控制 |
| R042 | 前置 | R043 | R042 校验 grantee_id 格式通过后，R043 才能正确创建 ACL 授权条目 |
| R044 | 互补 | R047 | R044 管 HTTP 请求层安全（401 + 超时），R047 管登录态校验，两者共同覆盖"请求层 + 登录态"前端安全 |
| R046 | 前置 | R047 | R046 确保 onMounted async 声明，R047 中的 checkLogin 异步校验才能正确执行 |
| R048 | 依赖 | R049 | R048 要求 PR 审查门禁，R049 要求分支保护，两者共同保障"代码审查流程"完整性 |
| R048 | 互补 | R036 | R048 管 rule/tool 资产同步门禁，R036 管 Service 注入路径一致性，两者从不同维度保障工程规范 |

## 变更日志

| 时间 | 变更 | 涉及节点 |
|------|------|---------|
| 2026-08-12 | 初始化项目级 graph.md，登记 R036（原有）+ R041-R049（从 project_memory.md 沉淀的 9 条项目特定规则）+ 6 条关联的全局规则节点 | R036, R041-R049 |
| 2026-08-12 | 建立项目规则间关系：R036↔R028/R025 互补、R041↔R043 互补、R042→R043 前置、R044↔R047 互补、R046→R047 前置、R048↔R049 依赖/互补 | R036, R028, R025, R041-R049 |
| 2026-08-19 | 按用户要求恢复 2026-08-07 设计（混合方案）：核心高频规则（fastapi/db/pytest/python）从全局复制到项目级 `.trae/rules/`，TRAE 自动加载；browser/terminal 留全局按需读取。在"关联的全局规则节点"表格加"项目级副本位置"列 | R025, R028, R031, R035, R037, R038 |
